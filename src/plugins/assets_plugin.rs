use crate::{domain::assets::*, infrastructure::assets::*};
use bevy::prelude::*;

pub struct AssetsPlugin;

impl Plugin for AssetsPlugin {
    fn build(&self, app: &mut App) {
        // Asset loading systems will be added here
    }
}
