[workspace]
resolver = "2"
members = [
    "game-engine",
    "grf-utils",
    "lifthrasir-ui",
    "lifthrasir",
]

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
bevy = { version = "0.18.1", features = [
    "jpeg",
    "trace",
    "trace_tracy",
    "bevy_remote",
    "file_watcher",
] }
secrecy = { version = "0.10", features = ["serde"] }
bevy_extended_ui = "1.6.0"

[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1
