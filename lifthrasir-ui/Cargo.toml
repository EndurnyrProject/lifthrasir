[package]
name = "lifthrasir-ui"
version.workspace = true
edition.workspace = true

[dependencies]
bevy = { workspace = true }
bevy_feathers = { workspace = true }
game-engine = { path = "../game-engine" }
iyes_progress = { workspace = true }
net-contract = { path = "../net-contract" }
leafwing-input-manager = { workspace = true }
secrecy = { workspace = true }
rfd = { workspace = true }

[features]
dlss = ["bevy/dlss"]
# Dev-only overlays (map light/effect gizmos). Enabled by the binary's `dev`
# feature; absent from release builds.
dev = []

[dev-dependencies]
lifthrasir-data = { path = "../lifthrasir-data" }
